# Copy this file to .env and replace the value locally.
# Never commit the real Materials Project API key.
#
#   cp .env.example .env      # then edit .env
#   set -a; source .env; set +a
#
# .env is gitignored. This file must only ever contain the placeholder.
MP_API_KEY=your_materials_project_api_key_here
